# Serve the management UI/API under a sub-path so nplus-infra's nginx can mount it
# at https://nplus.space/rabbitmq/ without colliding with other proxied services.
management.path_prefix = /rabbitmq
